import { defineConfig } from 'vitepress'

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "VitePress Starter Template",
  description: "An opinionated VitePress starter template",
  srcDir: 'content',
  outDir: 'dist',
})
